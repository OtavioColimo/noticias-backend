version: '3.8'

services:
  backend:
    build: .
    container_name: noticias-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
    restart: unless-stopped
    # Descomente abaixo para desenvolvimento em tempo real (hot reload)
    # volumes:
    #   - .:/app
    #   - /app/node_modules
